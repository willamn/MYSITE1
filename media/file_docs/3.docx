文档三：机器人卡车自动驾驶软件算法开发文档
1 文档基础信息

项目信息

内容说明

文档名称

机器人卡车自动驾驶软件算法开发文档

适用对象

算法工程师、软件开发工程师、后端研发工程师

开发框架

ROS2 Humble、Python3.9、C++17

算法版本

ALG-V1.1.0

2 软件开发环境配置

2.1 基础环境

操作系统：Ubuntu 22.04 LTS；编译工具：Colcon；仿真平台：Gazebo 11；可视化工具：RViz2；代码管理：Git。

2.2 项目目录结构

truck_auto_driver/
├── config/        # 算法配置文件（yaml参数）
├── src/           # 核心算法源码
│   ├── perception/ # 感知算法
│   ├── localization/ # 定位算法
│   ├── planning/  # 规划算法
│   └── control/   # 控制算法
├── launch/        # 启动脚本
├── msg/           # 自定义消息格式
└── scripts/       # 调试工具脚本

3 核心算法模块开发说明

3.1 环境感知模块

采用多传感器融合算法，融合激光雷达、摄像头、毫米波雷达数据，实现障碍物识别、行人检测、路况分类。算法采用YOLOv8轻量化模型，适配车载嵌入式算力；障碍物分类包含静态障碍物（石块、路障）、动态障碍物（行人、其他车辆），识别准确率≥95%。

ROS2话题：/perception/obstacle_detect，发布频率10Hz，输出障碍物坐标、尺寸、运动速度。

3.2 组合定位模块

融合RTK-GNSS、IMU、轮速里程计数据，采用卡尔曼滤波算法，实现全天候高精度定位。正常工况定位精度±2cm，无卫星遮挡场景；隧道、遮挡区域依靠IMU惯性补位，定位偏差≤15cm。输出车辆经纬度、车身姿态、行驶速度。

3.3 路径规划模块

分为全局路径规划与局部路径规划：全局路径采用A*算法，根据预设站点生成最优行驶路线；局部路径采用动态窗口法（DWA），实时规避突发障碍物，重新规划绕行轨迹。支持固定路线巡航、手动定点导航两种模式。

3.4 车辆控制模块

基于PID自适应控制算法，精准控制车辆转向、油门、制动；结合卡车大载重、大惯性特性，优化阻尼参数，抑制车身抖动。车速控制误差≤±0.3km/h，转向角度控制误差≤±1°，满足工业货运平稳性要求。

4 自定义API接口说明

接口名称

通信方式

输入参数

返回数据

车辆急停接口

ROS2 Service

emergency_flag（bool）

执行状态码、故障信息

路径下发接口

ROS2 Topic

路径坐标点数组

路径规划完成标识

车况查询接口

MQTT

车辆ID

车速、电量、故障码

5 代码开发规范

- 命名规范：函数、变量采用下划线命名法，类采用大驼峰命名，禁止无意义缩写。

- 注释规范：核心函数必须添加功能注释、入参出参说明，算法逻辑添加流程注释。

- 异常处理：所有硬件通信接口必须添加超时判断、异常捕获，避免程序卡死。
